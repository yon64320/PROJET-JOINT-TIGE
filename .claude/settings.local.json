{
  "permissions": {
    "allow": ["WebSearch"]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "CHANGED=$(git diff --stat HEAD 2>/dev/null | tail -1 | grep -oP \"\\d+ file\" || true); if [ -n \"$CHANGED\" ]; then echo \"{\\\"systemMessage\\\":\\\"Fichiers modifies detectes. Pense a /fin-session pour sync les .md.\\\"}\"; else echo \"{}\"; fi",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "prefersReducedMotion": false,
  "enabledMcpjsonServers": ["supabase"],
  "enableAllProjectMcpServers": true
}
